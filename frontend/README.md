# Kalshi Clone Frontend

Next.js UI for the Miami highest-temperature market clone.

## Run

```powershell
npm.cmd install
npm.cmd run dev
```

The frontend rewrites `/api/*` to the Django backend at `http://127.0.0.1:8000`.
